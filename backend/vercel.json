{
  "builds": [
    {
      "src": "src/index.ts",
      "use": "@vercel/node",
      "config": { "buildCommand": "tsc" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/index.js"  
    }
  ]
}